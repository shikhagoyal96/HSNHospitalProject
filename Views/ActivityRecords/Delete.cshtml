@model HSNHospitalProject.Models.ActivityRecords

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>ActivityRecords</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.activityrecorddate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.activityrecorddate)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.activityrecordrating)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.activityrecordrating)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <a href="@Url.Action("Index")" class="btn btn-default">Back to List</a>
            <input type="submit" value="Delete" class="btn btn-danger" />
        </div>
    }
</div>
